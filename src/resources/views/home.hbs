<div class="container">
  <div class="mt-4">
   <div class="row">
      {{#each posts}}
          <div class="card m-4" style="width: 18rem;">
            <img src="{{this.image}}" class="card-img-top card-img" alt="{{this.image}}">
            <div class="card-body">
              <h5 class="card-title">{{this.title}}</h5>
              <p class="card-text">{{this.decription}}</p>
              <a href="/courses/{{this.slug}}" class="btn btn-primary">Xem chi tiết</a>
            </div>
          </div>
      {{/each}}
   </div>
  </div>
</div>